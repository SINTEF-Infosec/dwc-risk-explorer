import { createTheme } from '@material-ui/core/styles';

export const themeDWC = createTheme({
    palette: {
        primary: {
            main: "rgba(31,39,71, 1)",
        },
        secondary: {
            main: "rgba(53,130,252, 1)",
        },
    },
});